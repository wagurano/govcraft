%section.page-block
  .ui.container.grid
    .column
      %h3
        = Project.model_name.human
        %small
          = link_to '새로운 프로젝트 시작하기', new_project_path if can? :create, Project

      .ui.four.column.grid.doubling.stackable
        = render @projects
