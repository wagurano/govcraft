= form_for @campaign do |f|
  .form-group
    = f.label :title
    = f.text_field :title, class: 'form-control'

  .form-group
    = f.label :body
    ~ f.text_area :body, rows: 8, class: 'form-control'

  .form-group
    = f.label :slug
    .input-group
      .input-group-addon
        #{request.base_url}/c/
      = f.text_field :slug, class: 'form-control'
    %p.help-block 영문자와 숫자, -만 넣어주세요. 이미 등록된 주소는 사용할 수 없습니다.

  .form-group
    = f.label :image
    = f.file_field :image, class: 'form-control'

  .form-group
    = f.label :discussion_enabled
    = f.check_box :discussion_enabled, class: 'form-control'

  .form-group
    = f.label :discussion_title
    = f.text_field :discussion_title, class: 'form-control'

  .form-group
    = f.label :poll_enabled
    = f.check_box :poll_enabled, class: 'form-control'

  .form-group
    = f.label :poll_title
    = f.text_field :poll_title, class: 'form-control'

  .form-group
    = f.label :petition_enabled
    = f.check_box :petition_enabled, class: 'form-control'

  .form-group
    = f.label :petition_title
    = f.text_field :petition_title, class: 'form-control'

  .form-group
    = f.label :wiki_enabled
    = f.check_box :wiki_enabled, class: 'form-control'

  .form-group
    = f.label :wiki_title
    = f.text_field :wiki_title, class: 'form-control'

  .form-group
    = f.submit '저장', class: 'btn btn-primary'
