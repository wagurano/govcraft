%section.page-block
  .container
    %div{ style: 'padding:14px; background: #fff; border-radius: 14px; text-align: center;'}
      %h3
        자, 이제 캠페인을 시작할까요?
      %p.lead{ style: 'margin-top: 28px' }
        진행 방법을 선택하세요.

      %p
        = link_to new_campaign_path(template: 'petition', project_id: params[:project_id]), class: 'btn btn-lg btn-info btn-xs-block' do
          서명 받기
        &nbsp;
        = link_to new_campaign_path(template: 'order', project_id: params[:project_id]), class: 'btn btn-lg btn-primary btn-xs-block' do
          촉구하기
        &nbsp;
        = link_to new_campaign_path(template: 'basic', project_id: params[:project_id]), class: 'btn btn-lg btn-success btn-xs-block' do
          목소리 모으기
        &nbsp;
        = link_to new_campaign_path(template: 'photo', project_id: params[:project_id]), class: 'btn btn-lg btn-danger btn-xs-block' do
          인증샷 모으기
        &nbsp;
        = link_to new_campaign_path(template: 'map', project_id: params[:project_id]), class: 'btn btn-lg btn-warning btn-xs-block' do
          지도 모으기
