.column
  .ui.card.fluid
    .image{ style: "height: 140px; background-image: url(#{timeline.image_url(:md)}); background-size: cover; background-position: center center;" }
      %a.ui.brown.ribbon.label= Timeline.model_name.human
    .content
      = link_to timeline.title, timeline, class: 'header'
      .meta
        %span
          #{date_f(timeline.created_at)}부터
          #{timeline.documents.count}개의 자료를 모았습니다.
      .description
      .extra
