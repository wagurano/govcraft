.container
  %h2= @discussion.title

  %div
    = simple_format @discussion.body

  %div
    = link_to '수정', edit_discussion_path(@discussion), class: 'btn btn-primary'
    = link_to '삭제', @discussion, method: :delete, data: { confirm: '삭제하시겠습니까?' }, class: 'btn btn-primary'