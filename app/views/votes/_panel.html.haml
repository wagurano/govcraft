.vote-panel
  %ul.list-inline
    - Vote.choice.values.each do |choice|
      %li
        = link_to polymorphic_url([choice.value, :votes], poll_id: poll.id), method: :post, class: 'btn btn-default btn-choice btn-lg' do
          %span.choice__label= choice.text
          %span.choice__count= poll.send("#{choice.value}s_count")
  - if poll.voted_by?(current_user)
    %div= link_to '투표 취소', cancel_votes_path(poll_id: poll.id), method: :post, class: 'text-muted'
