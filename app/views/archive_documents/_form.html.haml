= form_for @archive_document do |f|
  = f.hidden_field :archive_id

  .well
    %ul
      %li 날짜는 반드시 넣으시고, 시간은 넣지 않아도 됩니다.
      %li 출처는 반드시 입력해야 합니다.

  .row
    .col-sm-6
      .form-group
        = f.label :date
        = f.date_field :date, class: 'form-control'

    .col-sm-6
      .form-group
        = f.label :time
        = f.text_field :time, placeholder: 'hh:mm:ss', class: 'form-control'

  .form-group
    = f.label :tag_list
    = f.text_field :tag_list, placeholder: '태그1, 태그2, 태그3', class: 'form-control'
    %p.help-block
      콤마로 태그를 구분해서 넣으세요.

  .form-group
    = f.label :source_url
    = f.text_field :source_url, required: true, class: 'form-control'

  .form-group
    = f.label :image
    = f.file_field :image, class: 'form-control'

  .form-group
    = f.label :title
    = f.text_field :title, required: true, class: 'form-control input-lg'

  .form-group
    = f.label :body
    = f.text_area :body, class: 'form-control redactor'

  .form-group
    = f.submit '저장', class: 'btn btn-primary'
    = link_to '취소', :back, class: 'btn btn-default'