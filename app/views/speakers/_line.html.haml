= link_to speaker_path(speaker, agenda_id: agenda.id), class: 'item eight wide column' do
  %img.ui.avatar.image{ src: speaker.image }
  .content
    = speaker.name
    .text-muted= speaker.organization
