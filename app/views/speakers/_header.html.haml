%section.speaker-section
  .container-fluid
    .media
      .media-left
        = image_tag speaker.image.xs.url, class: 'media-object'
      .media-body
        %h3.media-heading
          = link_to speaker_path(speaker) do
            = speaker.name
          %small
            - speaker.positions.each do |position|
              %span.label.label-default.speaker__positions= position
        %p
          %span
            #{speaker.organization}
          - if speaker.election_region.present?
            %span
              #{speaker.election_region}
            지역
          %br
        .sns
          - if speaker.public_site.present?
            %a{ href: "#{speaker.public_site.to_s}" }
              %i.fa.fa-home
              웹사이트
          - if speaker.email.present?
            %a{ href: "mailto:#{speaker.email}"}
              %i.fa.fa-envelope
              이메일
          - if speaker.twitter.present?
            %a{ href: "http://twitter.com/#{speaker.twitter}", target: "_blank" }
              %i.fa.fa-twitter
              트위터
