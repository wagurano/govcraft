- speaker = opinion.speaker
.ui.raised.segment[opinion]
  %h4.ui.header
    = opinion.quote
  .opinion__body
    - if opinion.body.present?
      != opinion.body
    - else
      %p{ style: 'color: #aaa' } 상세한 답변은 아직 없습니다.
