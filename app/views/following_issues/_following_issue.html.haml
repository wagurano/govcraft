- issue = following_issue.issue
%span[following_issue]
  = link_to issue.title, issue
  %small.text-muted
    %i.fa.fa-user-o
    = issue.followers.count
  %small.buttons
    - if can? :destroy, following_issue
      = link_to '삭제', following_issue, method: :delete, data: { confirm: '정말 삭제하시겠습니까?'}
