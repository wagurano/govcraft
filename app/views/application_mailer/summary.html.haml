!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %meta{:charset => "utf-8"}/
    :css
      *{
          font-family:AppleSDGothic,"apple sd gothic neo","noto sans korean","noto sans korean regular","noto sans cjk kr","noto sans cjk","nanum gothic","malgun gothic",dotum,sans-serif;
      }
      .container{
          width:620px;
          margin:0 auto;
      }
      .header{
          text-align:left;
      }
      .header > p{
          font-size:13px;
      }
      .content h3{
          color:#445AB3;
      }
      .sub-content a {
          text-decoration: none;
      }
      .sub-title{
          background-color:#eee;
          color: #777;
          padding:15px;
          margin:8px 0px;
          box-shadow:1px 1px 0px 2px #ddd;
      }
      .content-title{
          margin-top:30px;
          border-top: 2px solid #eee;
          padding:10px;
          color:#989898;
          font-size:14px;
          font-weight:500;
      }
      .link-btn{
          font-size:12px;
          padding:3px 7px;
          border-radius:3px;
          color:#fff;
          margin:3px;
          text-decoration:none;
      }
      .footer{
          text-align:center;
          font-size:12px;
          margin-top:50px;
          margin-bottom:20px;
          line-height:20px;
      }
      .footer > h3{
          margin-bottom:3px;
      }
      .footer > h3 a {
          color:#4A5DD0;
          text-decoration:none;
      }
  %body
    .container
      .header
        %h2 우주당 뉴스레터
        %p
          한 주간 우주당에서는 이런 일들이 있었습니다.
          %br/
          잘 지내시죠? 보고 싶어요 :)
        %a.link-btn{:href => "http://wouldyouparty.org/", :style => "background-color:#4A5DD0", :target => "_blank"} 홈페이지
        %a.link-btn{:href => "http://wouldyouparty.slack.com", :style => "background-color:#e32072", :target => "_blank"} 슬랙
        %a.link-btn{:href => "https://trello.com/b/8yNqiEKP/-", :style => "background-color:#026AA7", :target => "_blank"} 트렐로
      - if @new_campaigns.any?
        .content
          .content-title
            캠페인
          .sub-content
            - @new_campaigns.each do |campaign|
              = link_to polymorphic_url(campaign) do
                .sub-title
                  = campaign.title
      - if @new_petitions.any?
        .content
          .content-title
            서명
          .sub-content
            - @new_petitions.each do |petition|
              = link_to polymorphic_url(petition) do
                .sub-title
                  = petition.title
      - if @new_polls.any?
        .content
          .content-title
            투표
          .sub-content
            - @new_polls.each do |poll|
              = link_to polymorphic_url(poll) do
                .sub-title
                  = poll.title
      - if @new_events.any?
        .content
          .content-title
            이벤트
          .sub-content
            - @new_events.each do |event|
              = link_to polymorphic_url(event) do
                .sub-title
                  = event.title
      .footer
        %h3
          %a(href='http://wouldyouparty.org')
            %small 디지털 직접 민주주의 플랫폼
            우주당
        = link_to '수신거부', edit_user_registration_url
