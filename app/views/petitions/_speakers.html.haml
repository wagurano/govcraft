.panel-body
  .panel-post__speakers
    %h4 관련 인물
    %p 이 서명과 관련된 국회의원, 행정관 등입니다.

    .row.masonry-container
      - @petition.speakers.each do |speaker|
        .col-md-3.col-sm-4.col-xs-12
          .card
            .card-image{ style: "height: 140px; background-image: url(#{speaker.image.md.url}); background-size: cover; background-position: center center;" }
            .card-content
              = link_to speaker.name, speaker, class: 'header'
              .card-meta
                %span
                #{speaker.organization}
              .card-description
              .card-extra
                %span.text-nowrap
                  = link_to new_comment_speaker_petition_path(@petition, speaker_id: speaker.id) do
                    %i.fa.fa-comment-o
                    - count = @petition.comments.with_target_speaker(speaker).count
                    의견보내기 #{"(#{count})" if count > 0}
                %br
                - if speaker.twitter.present?
                  %span.text-nowrap
                    - tweeter_msg = "[우주당] '#{@petition.title}'에 대해 의견을 묻고자 합니다"
                    = link_to "https://twitter.com/intent/tweet?text=@#{speaker.twitter} #{tweeter_msg}", target:'_blank' do
                      %i.fa.fa-twitter
                      멘션보내기
