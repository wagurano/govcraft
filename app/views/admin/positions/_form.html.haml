.panel.panel-default
  .panel-body
    = form_for [:admin, @position] do |f|
      .form-group
        = f.label :name
        = f.text_field :name, class: 'form-control'

      .form-group
        = f.submit '저장', class: 'btn btn-primary'
