.container
  %h3
    카테고리 목록
  %ul.list-group{style:"width: 200px"}
    - @project_categories.each do |project_category|
      %li.list-group-item.text-center
        = project_category.title
        = link_to project_category, method: :delete, data: { confirm: '카테고리가 삭제됩니다. 삭제하시겠습니까?' } do
          %i.fa.fa-times
  %hr
  %h3
    카테고리 추가
  = form_for ProjectCategory.new do |f|
    .form-group
      = f.label :title
      = f.text_field :title, class: 'form-control'

    .form-group
      = f.label :slug
      = f.text_field :slug, class: 'form-control'

    = f.submit '저장', class: 'btn btn-lg btn-primary'

